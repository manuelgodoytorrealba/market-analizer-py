# Market Analyzer

Sistema para detectar oportunidades de reventa comparando anuncios reales contra una referencia de mercado.

---

## 🚀 Quick Start (Team)

No importa si usas `python` o `python3`.

```bash
git clone <repo>
cd market-analyzer

python3 scripts/setup.py
python3 scripts/dev.py