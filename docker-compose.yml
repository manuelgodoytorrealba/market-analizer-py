version: "3.9"

services:
  app:
    build: .
    container_name: market-analyzer
    volumes:
      - .:/app
    working_dir: /app
    command: python -m scripts.cli serve --reload
    ports:
      - "8000:8000"
    env_file:
      - .env
